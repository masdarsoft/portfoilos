// app/types/index.ts
// Re-export mapped REST API types as canonical types for components
export type { RentalCategory, Review } from '../../lib/api/catalog';
export type { Tenant, TenantTheme } from '../../lib/api/tenants';
